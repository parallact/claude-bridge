{
  "name": "@parallact/claude-bridge",
  "version": "1.0.0",
  "description": "High-performance OpenAI-compatible proxy for Anthropic Claude API with OAuth token support. Zero CLI dependencies — direct API translation with full concurrency.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "node --experimental-strip-types --experimental-transform-types src/index.ts"
  },
  "keywords": ["claude", "anthropic", "openai", "proxy", "bridge", "api"],
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.112"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0"
  }
}
